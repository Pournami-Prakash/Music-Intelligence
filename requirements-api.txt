# Runtime-only dependencies for the hosted FastAPI backend.
# Compute and enrichment jobs use requirements.txt.

duckdb>=0.10.0
pandas>=2.0.0
pyarrow>=14.0.0
numpy>=1.26.0

# NOTE: boto3 is intentionally NOT here. It adds ~40-80 MB resident just to
# import + build an S3 client. The API downloads R2 objects with a lightweight
# SigV4-signed GET (src/storage/r2_download.py) using requests instead. boto3 is
# still required for uploads/compute jobs — see requirements.txt.
requests>=2.31.0
python-dotenv>=1.0.0

fastapi>=0.111.0
uvicorn>=0.29.0
pydantic>=2.0.0

# NOTE: faiss-cpu is intentionally NOT here. Vector similarity (Doppelganger /
# Transition) now runs against Upstash Vector over HTTP, so the API no longer
# loads the 393 MB FAISS index — this is what keeps the serving footprint small.
